Legacy JavaScript packages live here.

This directory groups older packages that are no longer part of the active
instrumentation path under `javascript-sdks/instrumentations/respan-instrumentation-*`.

The `respan-exporter-*` packages are kept here so active instrumentation and
legacy exporter code stay separated.
