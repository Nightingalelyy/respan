Active JavaScript instrumentation packages live here.

This directory mirrors `python-sdks/instrumentations/` and keeps the OTEL-based
instrumentation packages separate from legacy code under `javascript-sdks/legacy/`.
