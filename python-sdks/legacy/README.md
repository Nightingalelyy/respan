Legacy Python packages live here.

This directory groups older packages that are no longer part of the active
OTEL-based instrumentation path.

- `respan-exporter-*` packages use the older exporter-style integration path.
- `keywordsai-tracing` is also kept here as a legacy package.

Active OTEL-based integrations remain under `python-sdks/instrumentations/`.
