{
  "summary": "Add Python SDK auto-flush lifecycle support so root decorator spans and synthetic injected spans can export without explicit respan.flush(), make Respan.shutdown() drain telemetry after plugin deactivation, and align Python exported semantic span names with the JS lowercase/model-suffixed style",
  "packages": {
    "respan-tracing": "patch",
    "respan-ai": "patch"
  }
}
