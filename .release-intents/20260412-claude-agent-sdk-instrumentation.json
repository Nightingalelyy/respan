{
  "summary": "Add the Python Claude Agent SDK instrumentation package and patch respan-tracing to export its spans correctly",
  "packages": {
    "respan-tracing": "patch",
    "respan-sdk": "patch"
  }
}
